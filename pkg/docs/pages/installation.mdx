---
title: Installation
---

import Bleed from 'nextra-theme-docs/bleed'
import Callout from 'nextra-theme-docs/callout'

# Installation

`run` can be installed via various methods. 

## Package Managers

### Homebrew

If you are using macOS or Linux and already have [Homebrew](https://brew.sh/) installed, getting `run` is super easy.

```bash
brew install katallaxie/run/run
```